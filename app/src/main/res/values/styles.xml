<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="defaultEditStyle">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:padding">5sp</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="defaultEditTextStyle" parent="defaultEditStyle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_marginTop">@dimen/small_margin_size</item>
        <item name="android:inputType">text</item>
    </style>

    <style name="defaultEditTimeStyle" parent="defaultEditStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:inputType">time</item>
    </style>

</resources>